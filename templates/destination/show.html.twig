{% extends 'layout.html.twig' %}

{% block title %}Destination{% endblock %}

{% block main %}
    <h1>Destination</h1>

    <table class="w-full text-md bg-white rounded mb-4">
        <tbody>
            <tr class="border-b">
                <th class="text-left p-3 px-5">Id</th>
                <td>{{ destination.id }}</td>
            </tr>
            <tr class="border-b">
                <th class="text-left p-3 px-5">Des_dest</th>
                <td>{{ destination.desDest }}</td>
            </tr>
        </tbody>
    </table>

    {{ include('circuit/_back.html.twig') }}

    <a class="mr-3 text-sm bg-orange-500 hover:bg-orange-700 text-white py-1 px-2 rounded focus:outline-none focus:shadow-outline" href="{{ path('destination_edit', {'id': destination.id}) }}">edit</a>

    {{ include('destination/_delete_form.html.twig') }}
{% endblock %}

{% extends 'layout.html.twig' %}

{% block title %}EtapeCircuit{% endblock %}

{% block main %}
    <h1>EtapeCircuit</h1>

    <table class="w-full text-md bg-white rounded mb-4">
        <tbody>
            <tr class="border-b">
                <th class="text-left p-3 px-5">Duree_etape</th>
                <td>{{ etape_circuit.dureeEtape }}</td>
            </tr>
            <tr class="border-b">
                <th class="text-left p-3 px-5">Ordre_etape</th>
                <td>{{ etape_circuit.ordreEtape }}</td>
            </tr>
        </tbody>
    </table>

    {{ include('circuit/_back.html.twig') }}

    <a class="mr-3 text-sm bg-orange-500 hover:bg-orange-700 text-white py-1 px-2 rounded focus:outline-none focus:shadow-outline" href="{{ path('etape_circuit_edit', {'code_ville_etape': etape_circuit.code_ville_etape}) }}">edit</a>

    {{ include('etape_circuit/_delete_form.html.twig') }}
{% endblock %}
